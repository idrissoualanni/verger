# MEMORY.md — Le Verger

Checklist des modifications du projet.

- [création AGENT.md] → erreurs: aucune → fix: -
- [création MEMORY.md] → erreurs: aucune → fix: -
- [analyse PRD + CODING_AGENT_GUIDE.md] → erreurs: docx binaire non lisible directement → fix: extraction XML via python zipfile
- [maj stack AGENT.md] → erreurs: aucune → fix: - (décisions utilisateur : Neon + Workers + DO + Upstash Redis, zéro VPS)
- [re-séquencement plan AGENT.md] → erreurs: aucune → fix: - (pédagogie avant finances, 21 étapes / 6 phases, temps réel branché sur absences d'abord)
- [décisions utilisateur intégrées AGENT.md] → erreurs: aucune → fix: - (auth propriétaire seul au démarrage, pas de données fictives prod/landing, page Tarifs & Offres dédiée E6, factures PDF envoyées parents E16, 22 étapes)
- [clarifications E6 + E1] → erreurs: aucune → fix: - (page Tarifs statique, monorepo confirmé)
- [E1 setup monorepo] → erreurs: ENOSPC disque plein (0 octet libre) → fix: nettoyage pnpm store + npm cache + Temp + caches navigateurs ; création pnpm-workspace.yaml imbriqué par create-next-app cassant le workspace racine → fix: suppression du fichier ; scripts de build bloqués par pnpm 11 (esbuild, workerd) → fix: pnpm approve-builds --all ; flag shadcn -b changé (radix/base/aria) → fix: -b radix -p nova ; types partagés créés dans packages/shared (rôles, statuts, méthodes, types de notification)
- [E2 schéma Drizzle] → erreurs: circularité d'import schema↔index → fix: extraction des constantes dans constants.ts, index.ts ré-exporte ; migrations non générées (dépend de la config Neon en E3)
- [provisionnement Neon via CLI] → erreurs: package CLI inexistant (@neondatabase/cli 404) → fix: nom réel = neonctl ; auth navigateur timeout 60s → fix: API key dans .env.local ; channel_binding=require retiré de l'URL (compatibilité drivers) ; projet round-paper-78033335 (région eu-central-1) + base "verger" créés, migrations générées + push appliqué ✓ ; .env.local à la racine du monorepo (NEON_API_KEY + DATABASE_URL)
- [règle utilisateur] → erreurs: aucune → fix: - (convention AGENT.md : CLI officiel pour chaque outil — neonctl, wrangler, upstash)
- [E3 auth better-auth] → erreurs: ENOSPC répétés sur pnpm add better-auth → fix: installé après libération partielle (paquets en cache store) ; CLI better-auth bloqué par ignored builds (@prisma/client, better-sqlite3) → fix: approve-builds + fix placeholder "set this to true or false" dans pnpm-workspace.yaml ; shims .bin (tsx, drizzle-kit) non exécutables par node → fix: appeler les vrais binaires JS (tsx/dist/cli.mjs, drizzle-kit/bin.cjs) ; config CLI nécessite export `auth` statique → fix: auth.config.ts dédié (la factory createAuth(env) ne suffit pas) ; neon() rejette fallback postgres:// → fix: postgresql://u:p@localhost:5432/verger ; conflit snapshot drizzle (table users → user) prompt TTY impossible → fix: suppression drizzle/ + drop table users + enum user_role en base, re-generate + push --force ; adapter ne trouvait pas account/session/verification → fix: export * depuis auth-schema.ts dans schema.ts ; champ custom role ignoré (data:{role} vs plat) → fix: role À PLAT dans body signUpEmail ; wrangler.jsonc name invalide (@verger/api) → fix: verger-api ; account orphelin (user créé avant échec account) → fix: delete + re-run ; sortie create-owner avalée par pipe → fix: vérification directe en base
- [E3 test runtime] → erreurs: crash workerd (libuv assertion) lors d'un premier dev → fix: erreur réelle = name invalide ; sign-in curl OK, session + cookie OK, role PROPRIETAIRE confirmé ; Worker dev tourne sur localhost:8788 avec 5 vars .dev.vars ; dash() exige process.env sur Workers → fix: apiKey passée explicitement dans createAuth ; dashboard Sentinel : dash.better-auth.com + tunnel cloudflared/ngrok pour joindre le serveur local
- [tunnel Sentinel] → erreurs: cloudflared winget corrompu (binaire ~7 Mo, erreur 0xC1) → fix: binaire officiel GitHub 54 Mo dans ~/Downloads ; QUIC bloqué par le réseau (UDP 7844, timeout) → fix: --protocol http2 ; URL stable du tunnel : https://cork-cells-crafts-int.trycloudflare.com ; test public OK (/health + sign-in token)
- [dashboard Sentinel debug] → erreurs: "Invalid API key" mensonger (le plugin ne compare pas la clé : il fetch /api/auth/jwks et échoue si absent) → fix: plugin jwt() ajouté (auth.ts + auth.config.ts) ; table jwks générée par better-auth generate --output ../../packages/shared/src/auth-schema.ts (--config ./auth.config.ts obligatoire, --output relatif à apps/api) + drizzle-kit push → jwks 200 ; le plugin exige un JWT signé par le cloud BA (Authorization: Bearer, maxTokenAge 5m) — "Invalid API key" sans JWT = comportement normal ; vraie clé projet (ba_x1j86...) installée dans .dev.vars ; zombies workerd Windows (parents wrangler respawnent les enfants) → fix: taskkill parents + workerd par nom, nettoyage complet ; tunnel relancé vers 8787 : https://wear-hunt-figure-temperatures.trycloudflare.com (jwks 200 + sign-in OK via tunnel)
- [E3 clôturée — dashboard connecté] → erreurs finales: (1) binding BETTER_AUTH_API_KEY jamais transmis au plugin (index.ts passait un env partiel à createAuth) → log "API key hash is missing { apiKey: 'missing' }" → fix: c.env.BETTER_AUTH_API_KEY ajouté au handler + interface Bindings ; (2) dernière clé régénérée ba_wwfg3... installée + serveur relancé (log: "JWT is missing from header" = clé OK) ; (3) recherche GitHub issue #10574 : le dashboard valide sur <basePath>/dash/validate, requêtes parfois bloquées par Cloudflare Bot Fight Mode (pas notre cas : jwks 200 pour tous UA) ; CONNEXION RÉUSSIE par l'utilisateur → E3 terminée 11/08/2026
- [E4 spec design validée "1a 2b"] → erreurs: aucune → fix: - (découpage sénégalais pré-rempli ; style chaleureux verts clairs + arrondis marqués)
- [E4 setup front] → erreurs: composant avatar manquant (utilisé dans user-menu) → fix: shadcn add avatar ; next.config rewrites /api/* → http://localhost:8787 (proxy, cookies first-party, pas de CORS navigateur) ; middleware interceptait /api/* (redirigeait vers /login) → fix: matcher exclut api ; LayoutProps<"/(app)"> non supporté → fix: { children: React.ReactNode } ; role absent du type inféré côté client → fix: role optionnel dans SessionUser + fallback affichage
- [E4 API niveaux/classes] → erreurs: db.query...with sans relations définies → fix: relations drizzle levels↔classes ajoutées à packages/shared/src/schema.ts ; routes /api/levels + /api/classes CRUD protégées PROPRIETAIRE (requireOwner), DELETE refusé si rattaché ; CORS localhost:3000 + trustedOrigins ajoutés ; tsc OK
- [E4 front livré] → erreurs: aucune → fix: - (palette chaleureuse globals.css ; login page ; shell sidebar vert foncé + user menu ; dashboard KPIs ; CRUD niveaux/classes avec FCFA formaté, année scolaire auto "2026-2027", dialogs) ; web dev tourne sur http://localhost:3000, tests curl OK (login 200, get-session null sans cookie, dashboard 307 sans cookie, /api/levels 401 sans session)
- [landing page + register] → erreurs: phone non reconnu par le type signUp.email du client (additionalField côté serveur uniquement) → fix: cast via `as Parameters<typeof authClient.signUp.email>[0]` ; logo.png copié racine → apps/web/public/ ; middleware ajusté pour rendre / et /register publiques ; disque quasi plein (ENOSPC cache Turbopack) → persist désactivé, serveur OK ; toutes routes : landing 200, register 200, login 200, logo 200, dashboard 307 sans cookie
- [audit complet 14/08/2026 (3 subagents lecture seule)] → erreurs trouvées: escalation privilèges au signup (role à plat accepté) ; CORS/trustedOrigins en dur localhost:3000 ; 401 trompeur (rôle insuffisant renvoyé 401) ; QR absents des élèves créés avant colonne ; sidebar scan/paiements désactivés ; WS/broadcast sans auth ; polling provider cassé (/notifications/recent inexistant) ; offline-queue stub non branché ; audit_logs jamais écrits ; rate limit in-memory non purgé ; injection SQL potentielle (préfixe matricule/numéro) ; invoices stats sum() invalide (500) ; code mort invoices.ts L261 ; DELETE classe garde-fou factice ; secrets divergents .dev.vars vs .env.local ; notify.ps1 absent ; pas de git → fix: plan 18 phases écrit (docs/superpowers/plans/2026-08-14-corrections-completes.md), git init + commit de référence 248e50a
- [phase 0 du plan de correction] → erreurs: .gitignore ne couvrait pas *.log (api.log, dev.log, web.log staged) → fix: ajout *.log + git rm --cached ; notify.ps1 inexistant → fix: créé C:\Users\hp\notify.ps1 (NotifyIcon, testé exit=0) ; AGENT.md en CRLF → edits multi-lignes échouaient → fix: edits mono-ligne ; AGENT.md E4-E19+E22 cochés "recette en cours", E20/E21 marquées NON SATISFAITES + note d'état en tête → commit 33c1472
- [E22 déploiement frontend — Worker verger-frontend DÉPLOYÉ] → erreurs: (1) build OpenNext échouait au bundling esbuild (sharp .node inbundlable, require dans next/dist/server/image-optimizer.js) → fix: next.config.ts images.unoptimized + outputFileTracingExcludes sharp/@img + script scripts/clean-sharp.ts (purge sharp/@img des node_modules avant build) ; (2) build:cf initial produisait .open-next incomplet (pas de handler.mjs) → fix: rebuild complet purge .next/.open-next ; (3) premier deploy → 500 sur TOUTES les routes : "Dynamic require of /.next/server/middleware-manifest.json is not supported" (getMiddlewareManifest() de next-server fait require() dynamique, non supporté workerd — issue opennextjs-cloudflare #1232/#1258) → fix: script scripts/patch-opennext.ts court-circuite getMiddlewareManifest(){return null} (le middleware edge est déjà exécuté par worker.js en amont) ; (4) warn duplicate key "origin" dans le bundle (CORS env.ORIGINS écrase "*") → à investiguer dans le code source ; URL: https://verger-frontend.sabel.workers.dev (home/login/static 200, dashboard 307→/login OK) ; scripts package.json build:cf/preview/deploy/upload = clean-sharp → build → patch-opennext ; next réellement installé = 16.2.11 (package.json dit 16.2.11 maintenant, cohérent)
- [E22 suite — stratégie single worker adoptée par l'utilisateur] → erreurs: déploiement 1b88f3b9 (18:44) fait par l'utilisateur avec main=custom-worker.ts (wrapper .open-next/worker.js + export DO NotificationHub depuis src/lib/api/do) + catch-all src/app/api/[...all]/route.ts (forwarde /api/* vers app Hono src/lib/api/server.ts via getCloudflareContext) + wrangler.jsonc DO NOTIFICATION_HUB + migration v1 → 500 sur TOUTES les routes : le rebuild de 18:43 n'avait PAS exécuté patch-opennext.ts → getMiddlewareManifest require() revenu dans le bundle custom-worker.js → fix: tsx scripts/patch-opennext.ts + wrangler deploy → version d1d91946 ; vérifs post-fix: /, /login, /dashboard, /tarifs 200 ; /api/levels, /api/students 401 (Hono OK) ; /api/ws/notifications 400 (DO OK) ; /health 404 (normal, pas de route) ; middleware.ts devenu no-op pur (protection session edge RETIRÉE — dashboard accessible sans cookie, protection repose sur 401 API) ; ⚠ RESTE: /api/auth/session → 404 (better-auth répond, probable BETTER_AUTH_URL prod pointant vers l'ancien domaine verger-api au lieu de verger-frontend.sabel.workers.dev) ; secrets présents sur worker: BETTER_AUTH_SECRET, BETTER_AUTH_URL, DATABASE_URL
- [E22 suite — fix auth URL + route session] → erreurs: 404 /api/auth/session persistait après fix secret → cause réelle: dans cette version de better-auth la route session = /api/auth/get-session (200 OK), /api/auth/session n'existe plus ; paiements/page.tsx:200 appelait encore l'ancien chemin → fix: remplacé par /api/auth/get-session (rôle de l'utilisateur se charge à nouveau) ; secret BETTER_AUTH_URL corrigé → https://verger-frontend.sabel.workers.dev (wrangler secret put) ; NOTE: le fix paiements/page.tsx nécessite rebuild build:cf + deploy pour prendre effet en prod
- [RBAC centralisé + 401/403 + redirect login (plan 2026-08-19-rbac-401-403, branche feat/rbac-401-403)] → erreurs: subagent Tasks 2+3 échouait silencieusement (résultat vide) → fix: exécution inline ; imports multi-lignes cassés par le script de migration (ligne insérée au milieu du bloc import) → fix: script fix-import-placement.ts ; table Postgres `user` = mot réservé (DELETE sans quotes → erreur 42601) → fix: `"user"` quoté ; @neondatabase/serverless v1.1 n'accepte que les tagged templates → fix: sql\`...\` → résultat: matrice packages/shared/src/permissions.ts (ROLE_PERMISSIONS 5 rôles, hasPermission, modules navigation dashboard/notifications/journal pour la sidebar) ; helper requirePerm dans les 2 copies API (apps/web/src/lib/api/lib/permissions.ts + apps/api/src/lib/permissions.js) — 401 {"error":"Non connecté"} / 403 {"error":"Accès refusé pour votre rôle"} ; 62 guards migrés (9 routeurs web + parité apps/api) ; payments matrice fine (SECRETAIRE: read/create/update/delete, COMPTABLE: read/delete/stats — pas de route DELETE payments actuellement) ; travel PROPRIETAIRE+AGENT ; escalade fermée: databaseHooks.user.create.before force SECRETAIRE (3 configs), create-owner pose PROPRIETAIRE par update en base (export `user` singulier de auth-schema) ; client api.ts 401 → redirect /login?callbackUrl + 403 message clair ; sidebar dynamique par rôle (filtrage hasPermission, lien Paiements ajouté) ; messages « serveur API est-il démarré ? » supprimés ; DÉPLOYÉ version 762d7e38 et validé curl: non connecté → 401 Non connecté, signup role:PROPRIETAIRE forcé → session SECRETAIRE, SECRETAIRE → levels 403 / payments 200 / stats 403, compte test.rbac@verger.sn supprimé, 3 comptes PROPRIETAIRE présents en prod. Commits: 9139982 cb05695 d8615ee b61a00f 13cd465 9e2cd79 a85c73b d7530ad
- [fix 500 POST /students (matricule)] → erreurs: template drizzle sql plaçait ${year} en paramètre $1 DANS la regex quotée (jamais substitué) → fix: pattern entier passé en UN paramètre hors quotes dans students.ts L22 + invoices.ts L23 ; commit 4920815, rebuild+deploy version c06fd5ae, validé prod: POST /parents 201 + POST /students 201 avec matricule ELE-2026-001 généré
- [CI/CD GitHub Actions] → erreurs: CLOUDFLARE_API_TOKEN absent au premier push (step deploy fail) → fix: token créé par l'utilisateur puis édité pour ajouter Scripts des Workers/Modifier sur le compte (le token initial ne voyait AUCUN compte) ; DATABASE_URL uploadé vide sur le nouveau worker par un pipeline printf défectueux → 500 neon() "No database connection string" → fix: re-upload via fichier temp + stdin redirect ; wrangler tail instable sous Windows (ECONNRESET) → fix: tail en arrière-plan > log + sleep + curl + lecture fichier → erreurs visibles
- [worker renommé verger-frontend → verger] → erreurs: renommage = NOUVEAU worker sans secrets → fix: re-set BETTER_AUTH_SECRET/BETTER_AUTH_URL/DATABASE_URL via wrangler secret put --name verger ; WORKER_SELF_REFERENCE binding aussi renommé (sinon crash runtime) ; URL prod = https://verger.sabel.workers.dev validé (/, get-session, sign-in probe 200, route protégée 401) ; workflow .github/workflows/deploy.yml vert (build OpenNext + typecheck + deploy + smoke test 2m02s) ; secrets GitHub: CLOUDFLARE_ACCOUNT_ID + CLOUDFLARE_API_TOKEN posés ; commits poussés jusqu'à 03176d4 ; ⚠ ancien worker verger-frontend toujours EN LIGNE (rollback) — à supprimer sur confirmation utilisateur
- [nettoyage workers] → erreurs: aucune → fix: - (supprimés verger-frontend + verger-api sur confirmation utilisateur ; seul le worker "verger" reste en prod ; apps/api source conservée comme copie parité RBAC ; docs DEPLOY.md/AGENT.md décrivent encore l'ancienne archi 2 workers — à mettre à jour)
- [tests unit + E2E en CI] → erreurs: `pnpm -r test` exécutait aussi @verger/e2e (Playwright sans navigateur installé → fail) → fix: script racine "test": pnpm --filter '!@verger/e2e' -r test (filter AVANT -r, sinon "No projects matched") ; step workflow hardcodait encore `pnpm -r test` → fix: `pnpm test` ; push sans déclenchement CI car paths du workflow ne couvraient pas package.json racine → fix: paths += package.json/pnpm-lock.yaml/pnpm-workspace.yaml ; résultat run 32590178788 VERT: 9 tests shared + 4 tests web au step Unit tests, deploy OK, job e2e séparé 8/8 scénarios Playwright contre prod https://verger.sabel.workers.dev (auth setup PROPRIETAIRE probe.diag@verger.sn + 3 anon + 4 owner dont création élève matricule ELE-2026-xxx) en 19,4s ; secrets GitHub E2E_TEST_EMAIL/E2E_TEST_PASSWORD posés ; commits 7316358 3f6a035 40a8cba 24de521
- [chantier tests complets + incident CPU prod] → erreurs: Error 1102 CPU 10ms/requête plan gratuit (503 globales) → fix: getAuth mémoïsé (server.ts reconstruisait createAuth à CHAQUE requête), purge rateLimitMap>1000, pagination GET /payments {data,total,limit,offset} cap 500 — DÉPLOIEMENT EN ATTENTE (push non autorisé) ; bugs réels corrigés au passage: invoices total NaN + SQL parenthèse coalesce + ligne fantôme insert({}) + ROUTE SHADOWING /invoices/:id vs /stats (:id déplacé en fin), grades PATCH stockait "NaN" (garde-fou 400 entre 0-20) + bulletin rank 0 si findIndex -1 (→ null), staff stats byRole strings SQL non numérisées → fix: 280 tests unitaires verts/12 fichiers apps/web/tests/unit (pattern vi.mock db + fausse session middleware Hono, requirePerm réel) ; pièges tests: résultat select() = objet {from} jamais fonction directe, selects successifs = tableau chains.shift()
- [e2e multi-rôles + migration schema prod manquante] → erreurs: 7 DIVERGENCES schema.ts↔base Neon (travel_agencies vieilles colonnes contact/destination/description vs contact_name/phone/email/address/services/is_active ; audit_logs ABSENTE ; events.end_date, grades.school_year, invoices.due_date, staff.is_active, whatsapp_messages parent_id/error_message/created_at manquantes) → cause: modules développés sans re-push drizzle → fix: ALTER TABLE idempotents appliqués sur Neon (tables vides, zéro perte), vérif script getTableColumns vs information_schema = TOUT ALIGNÉ ; le 500 GET /api/travel/agencies en prod disparu (200 ✓) ; comptes sondes créés+promus SQL: probe.sec/compta/agent/ens@verger.sn (Probe1234!, role forcé SECRETAIRE au signup puis UPDATE) ; auth.setup.ts généralisé 5 storageState .auth/*.json, playwright.config helper roleProjects() 4 projects dependencies:["setup"], spec rbac-roles.spec.ts guard() skip par projet ; bug test "secretariat".toUpperCase()=SECRETARIAT≠SECRETAIRE → fix: map ROLE_BY_PROJECT ; crash workers chromium locaux (code 0x80000001) → fix: --workers=2 ; validation locale finale: 23 passed / 1 failed ATTENDU (test body.limit=5 exige la pagination NON ENCORE DÉPLOYÉE — passera en CI après deploy) / 36 skipped hors-rôle ; secret GitHub E2E_PROBE_PASSWORD posé ; DEPLOY.md réécrit single-worker + AGENT.md §8 à jour
